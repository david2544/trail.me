import React from 'react';
import { shallow } from 'enzyme';
import {{ properCase name }} from '../index';

describe('<{{ properCase name }} />', () => {
  // un-skip (xit -> it) this test when feature development is over. 
  // It is skipped to avoid the noise it creates during development
  xit('should match snapshot', () => {
    const wrapper = shallow(<{{ properCase name }} />);

    expect(wrapper).toMatchSnapshot();
  });

  it('should have an additional unit test specified', () => {
    expect(true).toEqual(false);
  });
});
